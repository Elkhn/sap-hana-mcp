[project]
name = "sap-hana-mcp-server"
version = "0.1.0"
description = "SAP HANA MCP Server using FastMCP"
requires-python = ">=3.12"

dependencies = [
    "fastmcp>=0.6.0",
    "hdbcli>=2.20.0",
    "python-dotenv>=1.0.0",
    "httpx>=0.27.0",
    "beautifulsoup4>=4.12.0",
]
